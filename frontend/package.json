{
  "name": "frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "concurrently \"npm run dev\" \"cd ../backend && node ./index.js\" ",
    "backend": "concurrently \"cd ../backend && npm install cors && npm install express && npm install mysql && npm install mysql && npm install jsonwebtoken && npm install nodejs-nodemailer-outlook && npm install dotenv\" "
  },
  "dependencies": {
    "axios": "^1.6.8",
    "jwt-decode": "^4.0.0",
    "pinia": "^2.1.7",
    "vue": "^3.4.15",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.3",
    "concurrently": "^8.2.2",
    "vite": "^5.0.11"
  }
}
